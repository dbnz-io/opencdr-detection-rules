{
  "rule_id": "023_correlation_credential_harvesting",
  "rule_kind": "correlation",
  "created_by": "opencdr",
  "description": "Multiple credential access signals (Secrets Manager + SSM) from the same actor within 5 minutes — indicates systematic secret harvesting.",
  "enabled": true,
  "severity": "CRITICAL",
  "notify": true,
  "response_module": "disable_user",
  "playbook": "The actor is bulk-reading secrets and parameters — a common post-exploitation step. Disable the actor, rotate all secrets and parameters they accessed, and identify what credentials may now be in attacker hands. Assess downstream systems that use those credentials.",
  "group_by": "actor.user_name",
  "time_window_seconds": 300,
  "threshold": 3,
  "signal_conditions": [
    {
      "field": "rule_id",
      "op": "in",
      "value": [
        "016_secretsmanager_accessed",
        "017_ssm_parameter_accessed"
      ]
    },
    { "field": "api.error_code", "op": "not_exists" }
  ]
}
